{
  "name": "final-project",
  "private": true,
  "description": "QR ordering — chạy script từ thư mục gốc",
  "scripts": {
    "dev:backend": "npm run dev --prefix qr-order-backend",
    "dev:frontend": "npm run dev --prefix qr-order-frontend",
    "install:all": "npm install --prefix qr-order-backend && npm install --prefix qr-order-frontend",
    "build:frontend": "npm run build --prefix qr-order-frontend"
  }
}
